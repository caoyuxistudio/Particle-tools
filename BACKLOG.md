# Backlog — 想做、还没做的功能

作者和 Claude 谈到、但决定先不做的功能都记在这里：是什么、为什么想要、打算怎么做、卡在哪。做完一项就从这里删掉，写进对应包的 CHANGELOG。已知的缺口（不是新功能，是没补齐的东西）在 `packages/studio/CLAUDE.md` 的「没做、记着的」。

格式：一项一节，标题写功能，下面四行——**要什么 / 为什么 / 怎么做 / 依赖**。

---

## 时间轴：快照，来回拖动（scrub）

- **要什么**：在时间轴上往回拖、点到任意一帧，画面立刻就是那一帧。
- **为什么**：现在的时间轴（2026-09-18）只能播放、暂停、停止回到开头。粒子模拟是一步步积分出来的，第 N 帧取决于前 N−1 帧，curl noise 的平流没有闭式解，所以不能直接"跳到"某一帧。
- **怎么做**：每隔一段（比如 1 秒）把 GPU 上的粒子缓冲（位置、速度、颜色、状态、init slot）拷一份存起来；拖到某帧时恢复最近的那份快照，只跑模拟不渲染，快进补齐剩下的几十步。Houdini / Blender 的模拟缓存就是这个思路。50 万粒子一份约 50 MB，10 秒的范围约 500 MB，桌面可接受；要有上限和淘汰。第一步可以先做不要快照的版本："回到第 0 帧，不渲染地快进到目标帧"。
- **依赖**：可复现的随机数（下一项）——否则恢复快照后重跑出来的不是同一段。

## 时间轴：可复现（带种子的随机数）

- **要什么**：同一份作品、同一个帧号，永远是同一张画面。
- **为什么**：序列帧导出要能重渲某几帧；快照要能从中间接着算；现在粒子出生用 `Math.random`（库里 26 处），每次跑都不一样。
- **怎么做**：库里加一个带种子的 PRNG（mulberry32 一类），出生位置、寿命、大小、旋转、颜色抖动全部走它；种子进 config（`seed`，`schema:`），stop / 重建时把 PRNG 重置到种子。real time 关掉时步长已经固定（1/fps），加上种子就是确定性的。视频色源要按帧定位（见下一项）才算完全可复现。
- **依赖**：时间轴（已做）。

## 序列帧导出（PNG / EXR 到本地文件夹）

- **要什么**：选一个文件夹，把时间轴范围内的每一帧渲出来写成 `frame_0001.png …`，无损，任意分辨率（不受窗口大小限制），不弹下载框。
- **为什么**：把作品交给 TouchDesigner / AE 做后期或成片。浏览器发不了 NDI（要原生 SDK 和 UDP），抓窗口是有损的还会掉帧；离线渲序列帧是无损的正路。
- **怎么做**：桌面 Chrome 的 File System Access API（`showDirectoryPicker`）直接写文件夹；时间轴 real time 关掉，每帧：步进模拟 → 把输出相机的管线渲进一张指定尺寸的离屏 RT（注意 `withDisplaySize`，后期 pass 的尺寸跟渲染器走不跟 RT 走）→ `readRenderTargetPixelsAsync` → PNG（canvas `toBlob`）或 EXR（three 的 `EXRExporter`，半浮点线性光）→ 写盘 → 下一帧。视频色源每帧 `video.currentTime = t` 等 `seeked` 再采样。"无损 JPEG"不存在，JPEG 一定有损。
- **依赖**：时间轴（已做）；要能重渲同一段还需要上一项。

## 色源上 GPU（作者 2026-09-18 决定不做，记录原因）

- **要什么**：把色源做成 GPU 纹理，粒子每帧按出生位置采当前视频帧，颜色实时跟着视频走。
- **状态**：**不做**。作者要的就是"出生时采一次、之后带着这个颜色"的效果，这是作品的语言，不是技术债。留在这里只为了以后有人再提时知道已经讨论过。

## 手机档的粒子预算

- **要什么**：同一份作品在桌面和手机上用不同的 `maxParticles` / `rateOverTime`。
- **为什么**：example-1-1 现在是 50 万 / 10 万（2026-09-18），桌面 59 fps；iPhone 上大概率撑不住。
- **怎么做**：config 里加一档按设备的覆盖（或者 player 按 `maxTouchPoints` 乘一个系数，Perf HUD 已经有 25% / 50% / 100% 的运行时杠杆），数值要在真机上用 Perf 面板量出来。
- **依赖**：iPhone 真机验证（`packages/studio/CLAUDE.md`「没做、记着的」）。
